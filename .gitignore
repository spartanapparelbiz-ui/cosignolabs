node_modules/
.next/
out/
.env
.env*.local
*.tsbuildinfo
next-env.d.ts
.DS_Store
.vercel
coverage/
test-results/
playwright-report/
.fonts/

# Per-run design-review captures (scripts/review-shots.mjs) — regenerable,
# and one run is 13MB. The curated screenshots/ folders stay tracked.
screenshots/review/
# Flow videos (scripts/capture-flows.mjs) — regenerable, and .webm in git is
# a repo nobody wants to clone.
screenshots/flows/
